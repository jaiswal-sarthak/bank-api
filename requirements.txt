# Requirements for the Bank API
# Note: runtime.txt controls Python runtime (set to Python 3.11 recommended)

# core
pydantic-core>=2.14.1,<3
pydantic>=2.5.0,<3

# FastAPI & ASGI server
fastapi==0.104.1
uvicorn[standard]==0.24.0

# GraphQL (optional)
strawberry-graphql[fastapi]>=0.200.0

# Database / ORM
sqlalchemy==2.0.20
aiosqlite==0.19.0

# Settings & env
pydantic-settings==2.1.0
python-dotenv==1.0.0

# Data processing / templates
pandas>=2.1.1,<3.0
jinja2==3.1.2

# Uploads / forms
python-multipart==0.0.6

# Testing / HTTP
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.25.1
